# 0.938.12 — 同步投递正门实测：v2 app 的 ctx.hooks 可用，v2 plugin 仍无

日期：2026-09-09 | 状态：**实测通过**
宿主：0.938.12-win32-x64（bundle 2026-09-09 10:05:12） | 插件：hana-downloader v0.14.0

## 0. 事件

宿主自动更新到 0.938.12（bundle mtime 09/09 10:05:12），bundle 里 `__sessionHooks` 命中 **0 次**，上次脚本化的魔改被覆盖。
插件 10:06:06 重启后实测降级：`ctx.hooks=undefined onDecision=undefined | globalThis.__sessionHooks=undefined` → `NO hooks channel → async-only`。
按用户决策：**不重建魔改**，转向评估官方正门。

## 1. 三条通道的当前状态

| 通道 | 状态 | 证据 |
|---|---|---|
| bundle 魔改 `globalThis.__sessionHooks` | 已被覆盖 | bundle `__sessionHooks` 命中 0 |
| v2 plugin `ctx.hooks` | 不存在 | `plugin-data/hana-downloader/v2-load-debug.log` 10:06:06 |
| v2 app `ctx.hooks.onDecision` | **可用** | `app-data/sync-probe/probe2.log` 02:14 起 |

## 2. 正门细节（0.938.12 bundle 行号）

- v2 app 的 `ctx` 组装在 L100435，共 19 个成员：
  `tasks/models/runtime/appEvents/tools/logger/bus/userInteraction/inputBanner/config/commands/hooks/media/network/routes/dataDir/documents/resources/storage`
- 开放给 app 的 hook 白名单 7 词（L98750-98763）：
  `agent/before-start`、**`agent/pre-step`**、`tools/pre-execute`、`tools/post-execute`、`messages/post-assistant`、`provider/before-request`、`session/input`
- `agent/pre-step`：invocation `{ messages, session }`，decision `{ messages }`（L100028），语义是「重写本回合要发给模型的消息」
- 能力字 `app/hooks.agent-pre-step`，默认 deny；注册总是成功，但只有 `Le.query({ domain:"app", id }, "app/hooks.agent-pre-step").decision === "allowed"` 时才会被咨询（L100062 附近）。授权入口：设置 → Security → App capabilities
- v2 plugin 调 `ctx.hooks.onDecision` 会被明确拒绝（L100044 报错文案）

## 3. sync-probe 实测（app 形态）

方法：把 `~/.hanako/apps/sync-probe` 改成 v2 app 形态（manifest 加 `icon`、capabilities 换 `app/hooks.agent-pre-step` + `app/tools.expose-to-model`），杀掉 app-host 子进程让宿主重启加载新代码。

注意：**app 子进程受 Node Permission Model 限制，只能写自己的 `app-data/{id}`**，诊断日志必须落在那里（写 `<workspace>/_temp` 会被静默拒绝）。

证据（`~/.hanako/app-data/sync-probe/probe2.log`）：

```
02:14:41 apply loaded. ctx keys(19)=[tasks,models,runtime,appEvents,tools,logger,bus,userInteraction,
         inputBanner,config,commands,hooks,media,network,routes,dataDir,documents,resources,storage]
02:14:41 hooks=object onDecision=function dataDir=~/.hanako\app-data\sync-probe
02:14:41 registered agent/pre-step adjudicator, disposer=function
02:14:45 agent/pre-step CONSULTED msgs=112 session=<hanako 会话 jsonl>
02:15:13 INJECTING msgs=118 -> 119 | [sync-probe 探针] ...
```

注入消息在下一轮 LLM 上下文可见 → **跨进程返回 `{messages}` 被宿主采用**，端到端成立。

## 4. 对 hana-downloader 的影响

- `index.js` 早已实现通道优先级：`ctx.hooks` 正门 → `globalThis.__sessionHooks` 魔改 → 都没有则 async-only；
  `lib/delivery.js` 的 `injectForSession(sessionPath, payload)` 已能把 pending 拼进 messages。
  **接正门不需要改投递逻辑**，缺的只是让入口以 app 形态运行。
- 路线 A（桥接）：插件保持 v2 plugin，新增极小 v2 app 只做 `agent/pre-step` 注入。下载内核/卡片/管理页全不动。
- 路线 B（全量迁 app）：与下载器天性冲突——`lib/dlcore.js` 依赖 `node:http/https/net`，app 子进程默认无 `--allow-net`，只能 `ctx.network.fetch` + `allowedHosts` 白名单（任意 URL 列不全）；`child_process` 需 `app/process.spawn`；落盘只能 `app-data` + 授权写根；WebView 卡片要重做成 v2 card/Preview。

## 5. 待验证

- app 子进程 idle 回收后，`agent/pre-step` 咨询能否唤醒它（影响每次请求延迟）
- 插件（v2 plugin，跑在宿主进程内）写 `app-data/{bridge}/queue.json` 是否被接受
- `ctx.bus` 能否跨 plugin/app 通信（可省掉文件轮询）

## 6. 成色标注

- **实测**：魔改覆盖、plugin 无 hooks、app ctx 19 成员、adjudicator 注册成功、每次 pre-step 被咨询、跨进程注入生效
- **代码/文档推断（未实测）**：bundle 行号对应的语义、能力字默认 deny 的授权面板路径、路线 B 的冲突清单
