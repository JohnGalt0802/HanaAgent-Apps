# 行尾统一为 LF（2026-09-19 加入）
#
# 背景：本机 core.autocrlf=true，checkout 时会把仓库里的 LF 转成工作区 CRLF。
# 而宿主运行副本是按原样复制的，于是「源码 vs 副本」的全量 hash 校验会冒出一大批
# 假差异（同一文件仅行尾不同），校验失去意义。
# 这里把规则钉死在仓库层面：仓库存 LF，检出也给 LF，不再漂移。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.exe binary
*.dll binary
